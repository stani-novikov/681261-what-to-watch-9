export enum AppRoute {
  Root = '/',
  AddReview = '/add-review',
  Film = '/films',
  MyList = 'my-list',
  Player = '/player',
  SignIn = '/sign-in'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
